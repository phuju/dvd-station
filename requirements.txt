# Core DiscStation host dependencies — must install.
pyserial>=3.5
mutagen>=1.45
requests>=2.28
yt-dlp>=2024.1.1
zeroconf>=0.131   # auto-discover a Wi-Fi appliance remote (DISC_REMOTE_HOST=auto, the default)
