# Expo SDK 57

This app targets **Expo SDK 57** (`expo ~57.0.20`, `react-native 0.86.3`).
Read the versioned docs at https://docs.expo.dev/versions/v57.0.0/ before
changing anything — the Expo API surface moves between SDKs.
