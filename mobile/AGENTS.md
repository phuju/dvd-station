# Expo SDK 55

This app targets **Expo SDK 55** (`expo ~55.0.0`, `react-native 0.83.10`).
Read the versioned docs at https://docs.expo.dev/versions/v55.0.0/ before
changing anything — the Expo API surface moves between SDKs.
